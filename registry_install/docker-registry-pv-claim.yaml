apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: images-pv-claim
spec:
  storageClassName: camel-nfs-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi